node_modules
dist
build
.env
.env.local
.env.*
# Exclude env files in ANY subdirectory (e.g. backend/.env) — bare .env only
# matches the build-context root, so the glob is required to keep secrets out
# of the image. Secrets are injected as Coolify env vars at runtime instead.
**/.env
**/.env.*
.git
*.log
.DS_Store
__pycache__
*.py[cod]
.venv
venv
.pytest_cache
*.sqlite
*.sqlite-journal
