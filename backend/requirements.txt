fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pandas==2.2.3
numpy==2.2.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy==2.0.36
cachetools==5.5.0
groq>=0.11.0
# psycopg2-binary is required only on Render (Postgres). Docker uses Python 3.12 where wheels exist.
# Local dev uses SQLite; install manually if you need Postgres locally: pip install psycopg2-binary
pytest>=8.0
